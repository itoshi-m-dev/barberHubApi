spring:
  application:
    name: scheduling-api

  data-source:
    url: jdbc:postgresql://localhost:5432/scheduling
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format-sql: true

